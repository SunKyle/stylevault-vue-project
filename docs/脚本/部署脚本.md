```bash
# 进入项目目录 
cd /Users/sunxiaokai/Desktop/stylevault-vue-project

# 使用 npm 打包
npm run build

# scp -r 本地dist目录路径 用户名@服务器IP:/var/www/
scp -r dist root@47.112.14.10:/usr/share/nginx/

# 远程重启nginx
systemctl restart nginx
```